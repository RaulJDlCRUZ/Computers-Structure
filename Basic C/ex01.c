#include <stdio.h>

// A firt functional program with variable definition, initialization and arithmetics

int main(){
    int a, b, c, sum;
    a=1; b=2; c=3;
    sum=a+b+c;
    printf("sum is %d\n", sum);
}